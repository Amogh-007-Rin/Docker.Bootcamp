# Lab 10 Solution

Cloud deploy always requires: build image → push to registry → define runtime (CPU, memory, env, ports) → create service. See `projects/project-08-cloud-deployment/` for provider-specific commands.
