# Lab 11 Solution

The builder stage trains and serializes the model; the runtime stage copies only `model.joblib` and inference code—keeping the final image smaller than installing build tools at runtime.
