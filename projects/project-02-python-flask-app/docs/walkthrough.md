# Project 02 Walkthrough

```bash
cd projects/project-02-python-flask-app
docker compose up -d --build
curl -s http://localhost:5000/
curl -s http://localhost:5000/
docker compose down
```

Second request should show `hits: 2`.
